services:
  backend:
    build:
      context: ./
    container_name: cine-backend
    restart: unless-stopped
    expose:
      - "3000"
    env_file:
      - .env.docker
    volumes:
      - ./uploads:/app/uploads
    networks:
      - cine-network

  nginx:
    image: nginx:latest
    container_name: cine-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.prod.conf:/etc/nginx/nginx.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - ./monitoring/.htpasswd:/etc/nginx/.htpasswd:ro
      - /var/www/certbot:/var/www/certbot:ro
    depends_on:
      - backend
      - grafana
    restart: unless-stopped
    networks:
      - cine-network

  prom-server:
    image: prom/prometheus
    container_name: prometheus
    expose:
      - "9090"            # internal only, not reachable from internet
    volumes:
      - ./prometheus-config.yml:/etc/prometheus/prometheus.yml
    networks:
      - cine-network

  loki:
    image: grafana/loki
    container_name: loki
    expose:
      - "3100"            # internal only
    networks:
      - cine-network

  grafana:
    image: grafana/grafana-oss
    container_name: grafana
    expose:
      - "3000"            # internal only, accessed via Nginx at /grafana/
    environment:
      - GF_SERVER_ROOT_URL=https://cinepulse.duckdns.org/grafana/
      - GF_SERVER_SERVE_FROM_SUB_PATH=true
      - GF_SERVER_DOMAIN=cinepulse.duckdns.org
      - GF_SERVER_PROTOCOL=http
      - GF_SERVER_ENFORCE_DOMAIN=false
    volumes:
      - grafana_data:/var/lib/grafana
    networks:
      - cine-network

networks:
  cine-network:
    driver: bridge

volumes:
  mongo_data:
  grafana_data: